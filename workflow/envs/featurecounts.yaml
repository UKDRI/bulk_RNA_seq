name: featurecounts_env
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - subread        # Includes featureCounts
  - awk            # For parsing outputs
  - coreutils      # For standard utilities like sed
  - python         # In case additional scripts are needed
