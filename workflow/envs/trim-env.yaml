name: trim-env
channels:
  - conda-forge
  - bioconda
channel_priority: flexible
dependencies:
  - python=3.8
  - pbzip2
  - pigz
  - fastqc
  - curl
  - git
  - r-base=4.2  # Specify a compatible R version
  - r-argparse=2.2.1  # Pin to a known compatible version
  - r-plotly=4.10.0   # Pin to a compatible version
  - r-ggsci=2.9       # Pin to a compatible version
