name: plots
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  # General dependencies
  - python=3.9
  - pandas
  - matplotlib

  # R dependencies
  - r-base
  - r-ggplot2
  - r-pheatmap
  - r-reshape2

  # Subread for featureCounts
  - subread
